# Runtime-only dependency set for the Python 3.12 AgentCore artifact.
# Model/inference-profile IDs are deployment environment variables, never code defaults.
bedrock-agentcore==1.22.0
boto3==1.43.92
pydantic>=2.11,<3
strands-agents==1.54.0
