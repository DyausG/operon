"""Operon A2A peers — the Governance and Monitoring agents exposed over the
Agent-to-Agent (A2A) protocol, so the Maintenance Agent (or any A2A client) can
consult them as peers. See ``a2a_app/server.py``.

A2A is for *agents* (these reason over a plan / the fleet); MCP is for *tools*."""
