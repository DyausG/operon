"""Sentinel MCP surface — exposes the agent's governed capabilities as MCP tools
so external agents/hosts (Claude Desktop, other agents, an A2A peer) can call the
same governed data model the in-app agent uses. See ``mcp_app/server.py``."""
