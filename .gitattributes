# Normalize text files to LF in the repo; check out native on each platform.
* text=auto eol=lf

# Windows launchers must stay CRLF so cmd.exe reads them correctly.
*.bat text eol=crlf

# Binary / generated artifacts — never diff or normalize.
*.joblib binary
*.db binary
